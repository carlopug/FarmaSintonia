<IfModule mod_rewrite.c>
    RewriteEngine On

    # Se la richiesta corrisponde a un file o a una cartella reale
    # (assets, favicon, ecc.) servila direttamente.
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [QSA,L]
</IfModule>
